import ProjectPrefix from './ProjectPrefix';

const StorageKeys = {
    'token': `${ProjectPrefix}_token`,
    'userInfo': `${ProjectPrefix}_userInfo`,
    'buttonList': `${ProjectPrefix}_buttonList`,
    'currentMenuKey': `${ProjectPrefix}_currentMenuKey`,
    'allDivisionQuery': `${ProjectPrefix}_allDivisionQuery`,
    'myDivisionPageIndex': `${ProjectPrefix}_myDivisionPageIndex`,
};

export default StorageKeys;